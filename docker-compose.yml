services: 
  mail_service:
    build: .
    environment:
      - SENDGRID_API_KEY=${SENDGRID_API_KEY}
      - RABBITMQ_HOST=${RABBITMQ_HOST}
      - RABBITMQ_PORT=${RABBITMQ_PORT}
      - RABBITMQ_USER=${RABBITMQ_USER}
      - RABBITMQ_PASSWORD=${RABBITMQ_PASSWORD}
      - FACTURATIE_TEMPLATE_ID=${FACTURATIE_TEMPLATE_ID}
      - CONTROLROOM_TEMPLATE_ID=${CONTROLROOM_TEMPLATE_ID}
      - FRONTEND_TEMPLATE_ID=${FRONTEND_TEMPLATE_ID}
      - QR_CODE_TEMPLATE_ID=${QR_CODE_TEMPLATE_ID}
    networks:
      - project_network 

#  mailing_heartbeat:
#    build:
#      context: ./publisher
#      dockerfile: Dockerfile  #Dockerfile specifiek voor heartbeat
#    environment:
#      - RABBITMQ_HOST=${RABBITMQ_HOST}
#      - RABBITMQ_PORT=${RABBITMQ_PORT}
#      - RABBITMQ_USER=${RABBITMQ_USER}
#      - RABBITMQ_PASSWORD=${RABBITMQ_PASSWORD}
#    networks:
#      - project_network
#    restart: always  # Optioneel, handig bij crashes


networks:
  project_network:
    driver: bridge